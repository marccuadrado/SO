#include <stdio.h>

int GuanyadorData {partidas, resultado, nombre)
	i=0;

	while(i<lista.num)
		if(strcpm (lista partida[i].fecha,fecha==0;
			sprintf
				return lista.persona[i].nombre

		i++;
	
	return 0;
}

