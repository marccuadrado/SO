#include <stdio.h>

SELECT partidas.ganador from (partidas) 	(guanyador data)
WHERE partidas.fecha = '2020-12-11'
	

SELECT distincs jugador.nombre from (partidas)	(nom guanyar + de 30p)
WHERE partida.puntos >= 30;
AND jugador.edad >= 18;
	

Select jugador.nombre FROM (jugador, partida)		(nom amb + punts)
WHERE partida.puntos = select MAX(partida.puntos) FROM (partida)
AND jugador.id = partidas.id

}

